mixin about-prod(data)
    +el({
        tag: 'section',
        class: 'about-prod',
        mod: data.mod,
        mix: data.mix,
        js: data.js,
    })&attributes(attributes)
        .about-prod__wrap
            h3.about-prod__heading Разработка и изготовление оборудования выполняется силами производственно-технического подразделения организации, включающего:
            +el({
                tag: 'ul',
                class: 'about-prod__list',
            })
                each item in aboutProdItems
                    +el({
                        tag: 'li',
                        mod: item.mod,
                        class: 'about-prod__item'
                    })
                        +item-w-arrow({
                            heading: item.heading,
                            text: item.text,
                        })
