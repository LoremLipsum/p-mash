mixin button-down(data)
    +el({
        tag: 'a',
        class: 'button-down',
        href: data.href,
        mod: data.mod,
        mix: data.mix,
        js: 'button-down',
        ariaLabel: 'Прокрутить вниз',
    })&attributes(attributes)
