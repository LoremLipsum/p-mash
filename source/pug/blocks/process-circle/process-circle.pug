mixin process-circle(data)
    +el({
        class: 'process-circle',
        mod: data.mod,
        mix: data.mix,
        js: data.js,
    })&attributes(attributes)
        if data.icon
            +el({
                tag: 'svg',
                icon: data.icon,
                class: 'process-circle__icon'
            })
